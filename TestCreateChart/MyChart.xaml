<UserControl x:Class="TestCreateChart.MyChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestCreateChart"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Canvas Grid.Column="0" Background="Red" Width="25"  Name="YHeaderCanvas">
            <Canvas.Children>
                <TextBlock Canvas.Left="0" Canvas.Top="25">Test</TextBlock>
            </Canvas.Children>
        </Canvas>
        <Canvas Background="Aqua" Name="ChartCanvas" Height="300" Width="400" Grid.Column="1" Grid.Row="0"></Canvas>
        <Canvas Background="Blue" Height="15" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Name="XHeaderCanvas"></Canvas>
    </Grid>
</UserControl>
